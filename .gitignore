# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Environment
.env
*.env.local

# Language state (set by setup.sh --lang)
.claude/.current-lang

# Claude Code local settings (contains MCP server permissions, may contain API keys)
.claude/settings.local.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Virtual environment
.venv/

# Raw data (large files managed separately)
raw/papers/*.pdf
raw/papers/**/*.pdf
raw/discovered/**/*.pdf
raw/discovered/**/*.tex
raw/web/**/*.html
raw/web/**/*.md
!raw/web/.gitkeep
raw/tmp/
!raw/tmp/.gitkeep

# User-private literature collections / personal research notes
01_文献/
raw/papers/patient-capital-literature/
raw/notes/research-intent.md

# Local Obsidian vault state (per-user; may contain plugin API keys / paths)
wiki/.obsidian/
wiki/.obsidian.bak.*/
wiki/.obsidian-*/
wiki/.trash/

# Local Claude Code state (per-user sessions, command history, plugin settings)
wiki/.claude/

# User wiki content (treat as private test data; users build their own wiki locally)
# Exception: preset schema cards (curated reference content shipped with the repo)
# are whitelisted one by one below the ignore rules.
wiki/papers/*.md
wiki/variables/*.md
wiki/datasets/*.md
wiki/models/*.md
wiki/mechanisms/*.md
wiki/hypotheses/*.md
wiki/identification/*.md
wiki/robustness/*.md
wiki/heterogeneity/*.md
wiki/tables/*.md
wiki/assumptions/*.md
wiki/propositions/*.md
wiki/concepts/*.md
wiki/topics/*.md
wiki/people/*.md
wiki/ideas/*.md
wiki/experiments/*.md
wiki/claims/*.md
wiki/Summary/*.md
wiki/foundations/*.md
wiki/outputs/*
# Derived graph state: regenerated by tools/research_wiki.py, lives only in the
# user's working tree. Untracked so branch switches can never clobber it.
wiki/graph/edges.jsonl
wiki/graph/citations.jsonl
wiki/graph/context_brief.md
wiki/graph/open_questions.md
# index/log carry the user's private research catalog — same treatment as the
# page content above. setup.sh seeds scaffold versions on first run.
wiki/index.md
wiki/log.md
!wiki/**/.gitkeep

# Preset schema cards — shipped with the repo as starter content
!wiki/datasets/csmar.md
!wiki/datasets/hua-zheng-esg.md
!wiki/datasets/wind.md
!wiki/datasets/cnrds.md
!wiki/datasets/dib.md
!wiki/datasets/tonghuashun.md
!wiki/datasets/wingo-financial-text-platform.md
!wiki/datasets/ccer-ownership-database.md
!wiki/datasets/cpdp-patent-database.md
!wiki/identification/difference-in-differences.md
!wiki/identification/propensity-score-matching.md
!wiki/identification/instrumental-variables.md
!wiki/identification/regression-discontinuity.md
!wiki/identification/two-way-fixed-effects.md

# User-owned literature sources (pdf rule exists below; tex was missing)
raw/papers/**/*.tex
# User notes may be symlinks into private local folders — never track
raw/notes/*.md

# Third-party personal skill manager state (not part of the product)
.agents/
skills-lock.json
# Symlinks into .agents/ created by the skill manager
.claude/skills/gpt-image-2
.claude/skills/kb-retriever
.claude/skills/web-design-engineer
.claude/skills/web-video-presentation
book/assets/_tmp/
book/assets/_manim_temp/
media/

# 临时编辑器 / 系统文件
*~
*.bak
.~lock.*

# JetBrains / VS Code workspace files
*.code-workspace

# Chat bridge dependencies (Agent SDK install is local-only; a 200MB binary
# once slipped into history through node_modules — never again)
tools/chat-bridge/node_modules/

# Quartz local site build (clone + node_modules + output, all local)
site/
